# Runtime dependencies for the inference service.
# Tested on Python 3.11-3.13.
#
# Install torch from the CPU wheel index so pip does not drag in the CUDA stack:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt
torch==2.13.0
torchvision==0.28.0
numpy==2.4.4
pillow==12.1.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
PyYAML==6.0.3
